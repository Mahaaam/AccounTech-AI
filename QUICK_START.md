# 🚀 راهنمای سریع - AccounTech AI

## نصب اولیه (فقط یک بار)

### روش 1: خودکار (توصیه می‌شود) ⭐
```
دابل‌کلیک روی: INSTALL.bat
```

### روش 2: دستی
```bash
# Backend
cd backend
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python init_db.py

# Frontend
cd frontend
npm install
```

---

## اجرای برنامه

### روش 1: اجرای کامل (Backend + Frontend) ⭐
```
دابل‌کلیک روی: START_ALL.bat
```

### روش 2: اجرای جداگانه
```
Backend:  دابل‌کلیک روی START_BACKEND.bat
Frontend: دابل‌کلیک روی START_FRONTEND.bat
```

---

## دسترسی به برنامه

بعد از اجرا، مرورگر خود را باز کنید:

```
http://localhost:5173
```

**رمز عبور پیش‌فرض**: `admin`

---

## اولین استفاده

1. **ورود**:
   - رمز: `admin`

2. **تغییر رمز** (توصیه می‌شود):
   - منو → تنظیمات → تغییر رمز عبور

3. **ایجاد حساب**:
   - منو → حساب‌ها → حساب جدید
   - نام: مثلاً "بانک ملی"
   - نوع: "دارایی‌ها"
   - سرفصل: "دارایی‌های جاری"
   - کد خودکار ساخته می‌شود!

4. **ثبت سند**:
   - منو → دفتر روزنامه → سند جدید
   - انتخاب: ثبت صوتی یا اسکن فیش

---

## رفع مشکلات سریع

### برنامه باز نمی‌شود
```
1. مطمئن شوید Backend اجرا شده (پورت 8000)
2. مطمئن شوید Frontend اجرا شده (پورت 5173)
3. مرورگر را رفرش کنید (F5)
```

### رمز عبور را فراموش کردم
```
1. فایل backend\app_password.txt را حذف کنید
2. Backend را دوباره اجرا کنید
3. رمز به admin ریست می‌شود
```

### دیتابیس خالی است
```
cd backend
venv\Scripts\activate
python init_db.py
```

---

## پورت‌های استفاده شده

- **Frontend**: 5173
- **Backend**: 8000

اگر این پورت‌ها اشغال هستند، برنامه‌های دیگر را ببندید.

---

## توقف برنامه

```
در پنجره ترمینال: Ctrl + C
یا بستن پنجره ترمینال
```

---

## پشتیبانی

اگر مشکلی پیش آمد:
1. README.md را مطالعه کنید
2. فایل‌های log را بررسی کنید
3. دیتابیس را پاک کنید و دوباره init کنید

---

**نکته مهم**: همیشه Backend را قبل از Frontend اجرا کنید!
